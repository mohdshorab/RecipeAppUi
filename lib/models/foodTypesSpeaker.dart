class FoodTypeSpeaker{

final String foodName;
  final String subText;
  final String imgURL;

  final int likes;
  final int forwarded;

  final String ingredients;
  final String instructions;

  FoodTypeSpeaker(this.foodName, this.subText, this.imgURL, this.likes, this.forwarded, this.ingredients, this.instructions);
}